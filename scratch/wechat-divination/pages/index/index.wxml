<view class="container">
  <!-- Background Decor: Ink Mountains -->
  <view class="bg-landscape"></view>

  <view class="title" bindlongpress="onTitleLongPress" style="color: {{titleColor}}">{{titleText}}</view>
  
  <view class="instructions">
    <view class="instruction-title">决策指南：</view>
    <view class="instruction-item">1. <text class="highlight">写下纠结</text>。把困扰你的选择题告诉硬币，或者拍张照。</view>
    <view class="instruction-item">2. <text class="highlight">寻找灵感</text>。抛掷硬币，让随机性帮你打破思维定势。</view>
    <view class="instruction-item">3. <text class="highlight">仅供参考</text>。结果源于随机概率，最终决定权在于你自己。</view>
    <view class="instruction-item">4. <text class="highlight">保持娱乐</text>。别太严肃，生活偶尔需要一点意外惊喜。</view>
  </view>

  <view class="input-section">
    <textarea class="question-input" placeholder="例如：今晚吃火锅还是减脂餐？要不要给前任点赞？..." bindinput="onInputQuestion" value="{{question}}" />
    
    <!-- Image Upload Control -->
    <view class="image-control-area">
      <!-- Left: Image Upload -->
      <view wx:if="{{tempImagePath}}" class="image-preview-box">
          <image src="{{tempImagePath}}" mode="aspectFill" class="image-preview" bindtap="previewImage"></image>
          <view class="remove-btn" bindtap="removeImage">×</view>
      </view>
      <view wx:else class="upload-btn" bindtap="chooseImage">
          <text class="camera-icon">📷</text>
          <text>拍个照</text>
      </view>

      <!-- Right: Direct Ask Button -->
      <view class="direct-ask-btn" bindtap="directAsk">
          <text class="magic-icon">💡</text>
          <text>智能分析</text>
      </view>
    </view>
  </view>

  <button class="start-btn" bindtap="startDivination">抛硬币决策</button>
</view>
